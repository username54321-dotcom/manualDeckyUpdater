{
  "type": "module",
  "bin": "main.ts",
  "scripts": {
    "build": "bun build --compile main.ts --outfile dist/deckyUpdater.exe ",
    "release:patch": "npm run build && npm version patch && git push origin HEAD --tags",
    "release:minor": "npm run build && npm version minor && git push origin HEAD --tags",
    "release:major": "npm run build && npm version major && git push origin HEAD --tags"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.4.3",
    "@octokit/rest": "^22.0.1",
    "commander": "^14.0.3",
    "dotenv": "^17.4.2",
    "extract-zip": "^2.0.1",
    "node-stream-zip": "^1.15.0",
    "ora": "^9.4.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  },
  "version": "1.0.0"
}
