{
  "type": "module",
  "bin": "main.ts",
  "scripts": {
    "build": "bun build --compile main.ts --outfile dist/deckyUpdater.exe ",
    "release:patch": "npm run build && npm version patch && git push --tags",
    "release:minor": "npm run build && npm version minor && git push --tags",
    "release:major": "npm run build && npm version major && git push --tags"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.4.3",
    "@octokit/rest": "^22.0.1",
    "commander": "^14.0.3",
    "dotenv": "^17.4.2",
    "extract-zip": "^2.0.1",
    "is-elevated": "^4.0.0",
    "node-stream-zip": "^1.15.0",
    "ora": "^9.4.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  },
  "version": "1.2.3"
}
